// Fill these in with your own Sanity project details (sanity.io/manage).
// Until SANITY_PROJECT_ID is set, app.js falls back to the bundled seed
// content in seed-data.js so the page still renders correctly.
window.SANITY_CONFIG = {
  projectId: 'hob493ap',
  dataset: 'production',
  apiVersion: '2024-01-01',
  useCdn: true,
};
